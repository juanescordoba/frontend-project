# frontend-project

Plantilla de frontend con **React + TypeScript + Vite**, **TailwindCSS**, **Clean Architecture** y **Jest** configurado.
Consume la API pública: https://swapi.dev/

## Cómo usar

```bash
npm install
npm run dev
npm test
```

La estructura sigue separación en `domain`, `application`, `infrastructure`, `presentation`.
